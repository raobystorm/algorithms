// POJ_3415.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include<stdlib.h>
#include<string>

using namespace std;

#define MAX		100000
char A[MAX], B[MAX] = {'\0'};
int K;

class substr{
public:
	string str;
	int count;
}substrs[100000];

int main()
{
	while (scanf_s("%d", &K) && K){
	
		getchar();

		gets_s(A);
		gets_s(B);

		
	}
	return 0;
}

