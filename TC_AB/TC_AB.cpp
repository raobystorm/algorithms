// TC_AB.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"

#include <iostream>
#include <string>
using namespace std;

class AB{
public:
	string createString(int N, int K){
		string s(N, 'A');
		int a = 0;
		while (a<N){
			int b = 0, la = 0, rb = 0;
			for (int i = s.size() - 1; i >= s.size(); i--){
				if (s[i] == 'B') {
					lb++;
					continue;
				}
				else{
					if
				}
			}
		}
	}
}

int _tmain(int argc, _TCHAR* argv[])
{
	return 0;
}

