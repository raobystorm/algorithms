#define MAX 100000

int majorityElement(int* nums, int numsSize)
{
  if(numSize <= 1) return *nums;
  
  int count[
  unordered_map<int, int> count;
  for (int i = 0; i < numSize; i++) {
    count[nums[i]]++;
    if(count[nums[i]] > (n-1)/2) return nums[i];
  }
}

int main(int argc, char *argv[])
{
  
  return 0;
}
