
#include <iostream>
#include <vector>

using namespace std;

struct node{
	int l, r, min, max;
	node* left, right;
};

int main(){
	int n = 0, m = 0;
	cin>>n;
	vector<int> a(n);
	vector<node>
	for(int i = 0; i < n; i++)
		scanf("%d", &a[i]);

	for()
	// build the segment tree here
	scanf("%d", &m);
	for()
}